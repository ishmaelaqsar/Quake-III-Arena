// Gate G1 script. Executed with `+exec smoke.cfg` by ci/smoke/run_smoke.sh.
// `wait N` holds command execution for N rendered frames. In timedemo mode the engine renders
// one frame per demo message, so the screenshot lands on the same demo frame every run.
demo four
wait 200
screenshot smoke
wait 5
quit
